<%= form_tag("/", method: "get") do %>
  <%= label_tag(:searchword, "Search for:") %>
  <%= search_field_tag(:searchword) %>
  <%= submit_tag("Search") %>
<% end %>

<% if @products %>
  <% @products.each do |product| %>
    <%= product.name %>
  <% end %>
<% end %>
